{% extends 'base.html.twig' %}
{% block body %}
    <div class="example-wrapper">
        <div class="flex justify-content-center">
            <div class="rounded-lg shadow-lg bg-white max-w-xl w-100">
                <div class="p-6">

                    <h1 class="mb-4 align-content-center text-black-50"> Nous Contacter !</h1>
        <div class="form-group">

            {{ form_start(form) }}
            {{ form_errors(form.email, {'attr' : {'class': 'alert bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative'}}) }}

            {{ form_row(form.nom, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.email, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.message, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group justify-content-center align-content-center ">
            <input type="submit" value="Send" class="btn btn-primary py-3 px-5 justify-content-center">
        </div>
        {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>

{% endblock %}
