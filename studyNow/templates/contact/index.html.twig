{% extends 'base.html.twig' %}
{% block body %}
    <div class="example-wrapper mt-5 mb-5">
        <div class="flex justify-content-center">
            <div class="rounded-lg shadow-sm bg-white max-w-xl w-100">
                <div class="p-6">
                    <div class="min-h-full flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
                        <div class="max-w-md w-full space-y-8">
                            <img src="{{ asset('./img/logo.png') }}" class="justify-content-center"
                                 alt="" loading="lazy" />
                            <div class="justify-content-center">
                                <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">remplisez les informations!</h2>
                            </div>
        <div class="form-group">

            {{ form_start(form) }}
            {{ form_errors(form.email, {'attr' : {'class': 'alert bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative'}}) }}

            {{ form_row(form.nom, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.email, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.message, {'attr': {'class': 'form-control block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none'}}) }}
        </div>
        <div class="form-group justify-content-center align-content-center ">
            <input type="submit" value="Send" class="btn btn-primary py-3 px-5 justify-content-center">
        </div>
        {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
        </div>
    </div>


{% endblock %}
